# Optional — add your GitHub personal access token to raise rate limit
# from 60 req/hr (unauthenticated) to 5,000 req/hr
# Create one at: https://github.com/settings/tokens (no scopes needed for public repos)
GITHUB_TOKEN=
